{
  "name": "herdr-worktree-include",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "lint": "oxlint --type-aware",
    "check": "tsc --noEmit && oxlint --type-aware && bun test"
  },
  "devDependencies": {
    "@types/bun": "^1.4.2",
    "oxlint": "^1.85.0",
    "oxlint-tsgolint": "^7.0.2002",
    "typescript": "^7.0.2"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/heyfirst/herdr-worktree-include.git"
  }
}
